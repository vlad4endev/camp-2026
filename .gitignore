# macOS
.DS_Store

# Учётки и живые данные лагеря — не в репозиторий
users.json
participants.json
signups.json
integrations.json
backups/

# Состояние синхронизации с Supabase: очередь неотправленных записей
# и зеркало данных лендинга — их пишет db.mjs на каждой машине своё
outbox.json
camp.json

# Токены: в файле панели, в переменных окружения
.env
